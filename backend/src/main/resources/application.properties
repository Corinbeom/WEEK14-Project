# ?? ?? (??? ?? ??)
server.port=8080

# MySQL ?????? ?? ?? (? DB ??? ??)
spring.datasource.url=jdbc:mysql://localhost:3306/week13?useSSL=false&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=qaz@137988
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA ?? (?? ??? update, ??? validate ??)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate Dialect ?? (Spring Boot 3.x ?? ??? ??? ??)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

jwt.expiration.ms=${JWT_EXPIRATION_MS}
jwt.secret=${JWT_SECRET}

file.upload-dir=/Users/hwaseongcityboy/Desktop/week13-pj/uploads

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB